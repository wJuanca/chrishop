---
console.log('javascript')
const { title, description } = Astro.props;
---


<div class="bg-white bg-opacity-90 rounded-lg shadow-lg p-6 transform transition duration-300 hover:-translate-y-2 hover:bg-pink-200 hover:bg-opacity-50 backdrop-blur-none">
    <h3 class="font-playfair text-xl sm:text-2xl font-semibold text-gray-800 mb-2">{title}</h3>
    <p class="text-gray-600 text-sm sm:text-base leading-relaxed">{description}</p>
  </div>